# ===========================================
# Toddler Schedule App - Environment Variables
# ===========================================
# Copy this file to .env and update the values

# Server port (default: 3001)
PORT=3001

# Database file path (default: ./toddler-schedule.db)
DB_PATH=./toddler-schedule.db

# For the React frontend, create client/.env with:
# REACT_APP_API_URL=http://localhost:3001/api
